'use strict';

describe('trackerApp.areas module', function() {

  beforeEach(module('trackerApp.areas'));

  describe('areas controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('AreasCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});